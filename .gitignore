# Machine-local Claude Code runtime state
.claude/scheduled_tasks.lock
.claude/settings.local.json
CLAUDE.local.md

# Secrets / local env (see .worktreeinclude — copied into worktrees, never committed)
.env
.env.*
.dev.vars
certs/

# Node / Next.js / Cloudflare (arrives with the W0-B skeleton)
node_modules/
.next/
.open-next/
.wrangler/
dist/
build/
*.log
tsconfig.tsbuildinfo

# Transient vitest JSON report consumed by scripts/gate-evidence.mjs
.vitest-gate-report.json

# Desktop agent (own toolchain — excluded from the root web toolchain; T0.1)
desktop-agent/src-tauri/target/
desktop-agent/src-tauri/gen/
desktop-agent/dist/
desktop-agent/node_modules/

# OS junk
.DS_Store
Thumbs.db
