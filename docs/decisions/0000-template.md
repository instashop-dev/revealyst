# NNNN — <title>

- **Status:** Proposed | Accepted | Superseded by NNNN
- **Date:** YYYY-MM-DD
- **Deciders:** <who>

## Context
What forced this decision? For a **post-freeze contract change**, state what the frozen
contract couldn't express and why retrofitting is unavoidable (rule 1).

## Decision
What we're doing, in the fewest words that are still unambiguous.

## Contracts affected
Which frozen artifacts change (from `CLAUDE.md` → Frozen contracts): typed interface,
schema, `tracked_user` semantics, credential shape, tenancy layer, metric catalog,
`connector-facts.md`. "None" if this is a non-contract decision.

## Workstreams to re-sync
Which lettered workstreams built against the old version and must be re-synced. Post a
re-sync note into each affected session (or, on a team, a PR comment/issue tagging owners).

## Consequences
Trade-offs accepted, follow-up work, what this closes off.
