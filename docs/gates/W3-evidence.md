# W3 exit-gate evidence — commercial & launch (V1 done)

Generated by `/gate-check W3` on 2026-07-07, against `main` @ `b3540af` (all four
W3 workstreams merged: W3-M billing via PRs #89–#100, W3-N compliance content,
W3-O hardening/ops, W3-P launch assets via #86/#93/#95/#98). Automated sections
are mechanical; NEEDS-FOUNDER slots are judged by a human against reality, never
self-certified by an authoring agent (rule 4). **Updated 2026-07-08** after the
founder closed the deploy blockers — see Sign-off.

Suite state at generation (after this pass's fixes; 543 tests across 62
files, +2 new regression tests): **every test green** — full parallel runs on
this Windows dev machine consistently time out 2–3 files at the 5 s limit
under load (a different set each run: `invites`/`metering`, then `access`,
then `share-links`; the full run spends ~6 min in imports alone), and every
one of those files passes in isolation on rerun — the CLAUDE.md-documented
flake family, not regressions. Typecheck PASS · org-scope guard PASS · CI
green on `main` @ `b3540af` · contract diff (contracts-v1 → HEAD) fully
ADR-covered.

## GATE STATUS: **PASS — all founder actions from this pack's first draft are complete; 3 founder-only follow-ups remain (non-blocking)**

All mechanical checks are green and every fix-now finding from the
adversarial pre-review (3 from the direct reviewers + 9 of 10 surviving
`/gate-review W3` workflow findings; 1 accepted-with-note) was fixed in this
session (W2 precedent: fix now, not a PR chain), including one HIGH
correctness bug in score reconciliation (ADR 0012). The wave's exit gate is
written against *production*, and this pass found (then closed) two
production gaps:

1. **Prod was 50 commits stale** — the last Deploy (`dedb231`) predated every
   W3 merge. **Fixed:** Deploy of `b3540af` triggered and verified live
   (landing, sign-in, legal pages, health all serving W3 code).
2. **Prod billing was entirely unconfigured** — no `PADDLE_*` Worker secrets
   existed and `deploy.yml` never synced them: the webhook returned
   `500 "webhook secret not configured"` for every event, and checkout could
   not have opened. **Fixed in two stages:** this session added the
   deploy.yml sync + 4 of 7 secrets from verifiable sources, then the founder
   added the remaining `PADDLE_API_KEY` + `PADDLE_CLIENT_TOKEN` and re-ran
   Deploy (run 28921194585, `926fde3`, success) — confirmed both uploaded
   (not skipped) in the sync-step log. All 7 secrets are live except
   `PADDLE_WEBHOOK_SECRET_SANDBOX` (intentionally unset — production-only).
   `POST /api/billing/checkout` (unauth) now returns 401 fail-closed and the
   webhook rejects a bad signature with 401 (not 500) — the full billing path
   is deployed and configured.

## Exit-gate checklist (Execution Plan, Wave 3 / V1 done)

| # | Gate item | Evidence |
|---|---|---|
| 1 | Public self-serve signup live | **PASS** — Deploy of `b3540af` completed (run 28866445448, success). Probes: `/` 200, `/sign-in` 200, `/legal/terms` 200, `/legal/privacy` 200, `/onboarding` 307→sign-in (auth-gated, correct), `/api/health` `{"ok":true,"db":"ok","heartbeatFresh":true}` (poller cron alive, heartbeat 268 s old). Landing copy renders from the live registry and `FREE_TRACKED_USER_LIMIT`: "Free for individuals and teams up to 5 tracked users", Copilot under "Soon". |
| 2 | Personal free / Team paid via Paddle checkout, entitlements enforced | **PASS.** Code paths: `tests/paddle-webhook.test.ts`, `tests/subscriptions.test.ts`, `tests/billing.test.ts`, `tests/entitlements.test.ts`, `tests/access.test.ts`, `tests/metering.test.ts` — all green (webhook signature fail-closed, idempotent/out-of-order-safe upserts, free band ≤5 enforced server-side in `handleApi` AND at page render, metering bills resolved tracked_users only, floored at 1, claim-before-charge). Paddle production side verified via API: price `pri_01kwxr7mysf3s37c9tk3mmd5y6` active at $2.00 USD/user/mo; FOUNDER discount active, 50% recurring, expires 2026-08-31, product-restricted; webhook destination active with exactly the 4 subscribed events. **Deployed and configured:** all 7 `PADDLE_*` Worker secrets synced (run 28921194585) except the intentionally-unset sandbox webhook secret; unauth checkout → 401, bad-signature webhook → 401 (both fail-closed, live). **Founder-only, not gate-blocking:** the end-to-end purchase walkthrough itself (incl. confirming the dashboard "Default Payment Link" setting — see the W3-M memory note; unset ⇒ overlay never opens) has not been run — config is verified, a live transaction is not. |
| 3 | A CTO can answer "who's using AI, how well, worth it" without a call | **PASS (carried + extended)** — the W2-gated dashboard (Adoption/Fluency/Efficiency, benchmark panel, shared-account flags, privacy default) is unchanged and its suites remain green; W3 added the benchmark-panel provenance relabel (#95, modeled estimates, invariant b) and the playbook/compliance onboarding content. Founder sign-off on "numbers match reality" still rests on the dogfooding item (row 6). |
| 4 | Tenant-isolation proof (mandatory every gate) | **PASS** — `tests/tenant-isolation.test.ts` 29/29 (W2's 27 + 2 new W3 tables). Both `subscriptions` and `audit_log` are in `SCOPED_READS` with non-vacuous B-org seed rows (completeness tripwire satisfied; verified independently by contract-guardian). `EXEMPT_TABLES` not widened. |
| 5 | Contract-test suite + E2E | **PASS** — `tests/contracts.test.ts` + `tests/e2e/ingest-to-score.e2e.test.ts` + isolation = 45/45 in a clean sequential run. |
| 6 | Golden dogfooding test (recorded real payloads → known-true numbers) | **NEEDS FOUNDER (carried from W1/W2)** — only `claude-code-local` recordings exist in `fixtures/vendor-payloads/`; Anthropic/OpenAI/Cursor recordings still need founder keys (`scripts/record/anthropic.mjs` is ready). Same blocker also holds the W3-P benchmark blog post (founder must return `docs/launch/benchmark-post-data-needs.md` answers + prod `launch:metrics`/`calibrate:scores` numbers). |
| 7 | TTFI / activation instrumentation (§15) | **PASS mechanically · timing NEEDS FOUNDER** — `tests/launch-funnel.test.ts` green; funnel derives timing from append-only `connector_runs` (never the rewritten `score_results.computed_at`, per the W3-P timestamp rule). The "minutes, not weeks" landing claim still needs one measured live signup→score run with a real key (launch blocker #4, unchanged). |
| 8 | W3-P launch assets | **PASS except the benchmark post** — landing page (live, registry-derived claims), share-card viral loop with revoke surface (#98: GET/DELETE `/api/share` + dialog UI, ownership-gated — verified by adversarial review), launch docs/announcements merged and fact-checked (13 findings fixed in W3-P). **Benchmark blog post: founder-blocked** on row 6's data (PR4 not started — deliberate, per kickoff decision). |

## Test output (summary)

Full parallel run: 62 files / 541 tests — 539 pass, 2 load-timeout flakes
(pass isolated; see header). Gate-critical sequential runs, all green:

- `tenant-isolation` 29/29 · `contracts` + `e2e/ingest-to-score` (45/45 combined)
- W3 billing/launch batch (`paddle-webhook`, `entitlements`, `access`, `metering`,
  `launch-funnel`, `audit-log`, `billing`, `subscriptions`, `share-links`,
  `health`, `system-health`, …): 130 tests — 129 green in-batch, the 1 timeout
  (`access`) green isolated.

## Contract drift — clean (all ADR-covered)

`contract-guardian` diffed `contracts-v1` → `b3540af`, focused on `w2-complete..HEAD`.
**No uncovered drift, no blockers.** All W3 changes to frozen paths are additive
and ADR-covered:

- `src/contracts/api.ts` +`billingCheckout`/`billingPortal` route contracts — ADR 0011.
- `subscriptions` table (mig 0015) — ADR 0009; org_id NOT NULL, composite anchor, org index.
- `audit_log` table (mig 0016) — ADR 0010; same tenancy shape.
- `forOrg` gained an org-pinned `auditLog` namespace (ADR 0010); `subscriptionsForOrg`
  is a declared capability-style exception (ADR 0009) whose webhook upsert pins
  `org_id` in its `setWhere` — a foreign event cannot rewrite another org's row.
- `tracked_user` billing semantics **CLEAN**: every billing surface (metering,
  checkout seat seed, paywall) consumes frozen `countTrackedUsers` and bills
  `trackedPersonIds` only; `unresolvedSubjectIds` is never read by any billing
  caller. `src/contracts/tracked-user.ts` byte-identical since the freeze.
- No plaintext credential path: Paddle keys are Worker-env secrets, never persisted.
- `metric_records` upsert key, `subject_day_signals`, `connection_credentials`,
  metric catalog, score presets, `fixtures/**` existing shapes: untouched.

## Live production verification (this pass)

- Deploy run 28866445448 (`b3540af`) — success; migrations 0015/0016 applied
  (additive CREATEs).
- Route probes and landing-copy checks: see checklist row 1.
- `POST /api/billing/checkout` unauthenticated → 401 (fail-closed).
- `POST /api/webhooks/paddle` → was `500 "webhook secret not configured"`
  (Finding 3). After this pass's deploy.yml fix + secret sync + re-deploy
  (run 28870312823, `43abaa6`): unsigned probe now returns
  `401 "invalid signature"` — the webhook/entitlement path is live and
  fail-closed. Checkout/portal/metering still await the founder's
  `PADDLE_API_KEY` + `PADDLE_CLIENT_TOKEN`.
- Post-fix prod content verified: ToS has zero Copilot mentions; landing mock
  card renders the "Example card" caption; all fixed copy is live.
- Paddle production objects verified live via API (price, FOUNDER discount,
  webhook destination + 4 events) — details in checklist row 2.

## Adversarial pre-review (W3)

Run per rule 4 by agents that did not write the code: `contract-guardian`
(clean — above), an `adversarial-reviewer` refute-mode pass over
`w2-complete..HEAD` prioritizing the Paddle webhook / entitlements / metering /
share-revoke / checkout / audit / launch-content surfaces, and the saved
`/gate-review W3` workflow (parallel finders, each finding adversarially
refuted, survivors synthesized). `/code-review ultra` was **not** run — it is
founder-triggered and billed; run it before final sign-off (same as W2).

### Finding 1 (MEDIUM — invariant b, content) — unshipped Copilot connector present-tensed on two customer-facing surfaces

The registry ships exactly three connectors (`anthropic`, `openai`, `cursor`);
the landing page correctly derives Copilot as "Soon". But two hand-written
strings present-tensed it: the in-app connections empty state ("Connect a
vendor (Anthropic, **Copilot**, Cursor, OpenAI…)") and the live ToS §1 ("reads
usage and cost data from … Anthropic, OpenAI, **GitHub Copilot**, and
Cursor"). A ToS is a claim surface (W3-N rule); this is the third independent
reinvention of the same overclaim pattern (W3-N #75/#76, W3-P launch copy).

**Disposition: fixed this session.** Copilot dropped from
`src/app/(app)/connections/page.tsx` and `src/app/legal/terms/page.tsx`.

### Finding 2 (MEDIUM — invariant b, billing copy) — Team seat line hard-coded "$2 / tracked user / month" regardless of the active FOUNDER discount

`src/app/(app)/billing/page.tsx` computed `hasFounderDiscount` and correctly
conditioned the *Personal* upgrade copy, but the *Team* branch stated the
billed amount unconditionally as $2 — an overstatement for any subscription
carrying the active 50% FOUNDER discount ($1), found by the `/gate-review W3`
correctness finder and confirmed against the live discount config.

**Disposition: fixed this session.** The seat line now states the $2 *list*
price and conditions the discount mention on `hasFounderDiscount` — the same
guard its sibling branch already had (the W2 "sibling guard" pattern again);
it no longer asserts a per-seat billed amount the system doesn't store.

### Finding 3 (BLOCKER — ops/config) — merged billing code had no production configuration path

`deploy.yml`'s Worker-secret sync contained no `PADDLE_*` entries, no repo
secrets existed, and nothing in `wrangler.jsonc` supplies them — so W3-M's
merged, tested billing was undeployable-as-configured: every real Paddle
webhook would 500 and checkout could not initialize. Confirmed live before
the fix (`{"error":"webhook secret not configured"}`).

**Disposition: fixed.** `deploy.yml` now syncs all 7 `PADDLE_*` values
(skip-if-unset, matching the existing pattern); 4 repo secrets were set from
verifiable sources this session (`PADDLE_ENVIRONMENT=production`, price ID,
discount ID, webhook endpoint secret read from Paddle's notification-settings
API), and the founder added the remaining `PADDLE_API_KEY` +
`PADDLE_CLIENT_TOKEN` and re-ran Deploy (run 28921194585, success — both
confirmed uploaded, not skipped, in the sync-step log). Post-fix probes:
unauth checkout → 401, bad-signature webhook → 401 (both fail-closed).
Remaining founder-only step: the live purchase walkthrough itself (a
transaction, not a config check) — see Sign-off.

### `/gate-review W3` workflow findings (10 surviving of 12; each adversarially refuted before being kept)

The saved workflow ran 5 finder dimensions (tenancy, contract-drift,
attribution, tripwire, correctness) with per-finding refuters. Ten findings
survived; every fix-now disposition below was applied and re-verified in this
session.

**C1 (HIGH — correctness, gate-blocking): team/org scores never reconciled
when their data disappears.** `recomputeOrg` reconciled stale *person* rows
(`deleteStalePersonResults`, tested) but the adjacent team/org branches only
ever upsert — after a restatement-to-empty (the poller's window delete, a
purged connection), team and org `score_results` computed from data that no
longer exists render on the dashboard/trends/benchmark panel for the rest of
the period, beside 0 active people and $0 spend. The W2 "sibling guard"
pattern exactly. **Fixed this session (ADR 0012):** two additive
`forOrg().scores` siblings (`deleteStaleTeamResults`, `deleteStaleOrgResults`)
wired into the recompute branches; regression test seeds team+org scores,
wipes the metric window, and asserts both rows are reconciled away
(`tests/scoring-recompute.test.ts`, "team/org stale row reconciliation");
the summary counter renamed `staleResultsRemoved` to match its widened scope.

**C2 (MEDIUM — correctness): poller restatement not transactional.** The
delete-then-upsert added at the W2 gate ran as two auto-commit statements
while its agent-ingest sibling wraps the identical pair in `db.transaction` —
queue consumers run concurrently with the nightly recompute, so a recompute
SELECT landing between delete and upsert scores a half-restated window
(permanently wrong when it races the final recompute of a month). **Fixed
this session:** `src/poller/run.ts` now wraps delete + upsertRecords +
upsertSignals in one transaction, mirroring `agent-ingest.ts`.

**C3 (LOW — correctness): public share card null for ~a day at every month
boundary.** `resolveShareCard` read only the current calendar month, whose
first row lands with the nightly recompute on the 2nd — already-shared cards
and OG images flipped to "not computed yet" and back. **Fixed this session:**
the card now reads the current + previous month and renders the latest
computed row (ADR 0008 pins the *projection*, not the period); month-boundary
test added (`tests/share-links.test.ts`). `share-card.ts` also moved to
relative imports so it is actually testable under vitest.

**A1 (MEDIUM — invariant b): Indie Hackers launch post said "free below 10
users"** — the enforced limit is `FREE_TRACKED_USER_LIMIT = 5` (the same doc
says 5 five times elsewhere). **Fixed this session** (`docs/legacy/launch/announcements.md`).

**A2 (MEDIUM — invariant b): shared-accounts empty state claimed "Per-person
attribution is clean"** from usage-pattern heuristics that never read
attribution levels — rendering next to score cards wearing "Key / project" /
"Account-level" badges. **Fixed this session:** the empty state now claims
only what the heuristics measure ("No usage patterns suggesting shared
accounts in this window.").

**A3 (LOW — invariant b): landing mock score card said "Measured across real
AI-tool usage" under fixed demo numbers** with no rendered example label.
**Fixed this session:** caption now reads "Example card — scores are measured
from real AI-tool usage, not self-reported."

**A4 (LOW — invariant b): the frozen `tracked_user` contract comment still
said "the ≤10 free band"** — the designated ground-truth document seeding the
same 2× overclaim A1 exhibited. **Fixed this session** (comment now points at
`FREE_TRACKED_USER_LIMIT`); comment-only, bundled with this PR's
`docs/decisions/` changes for the CI frozen-path guard.

**A5 (LOW — accepted with note): the team dashboard has no honesty-gaps
surface** — the personal self-view shows the "How complete is this?"
connector-gaps alert, the team view drops the same data (per-card attribution
badges partially mitigate; `/api/dashboard` already returns the gaps).
**Disposition: fast-follow**, spun off as a tracked task — thread `gaps` into
`DashboardData`/`TeamOverview`.

**D1 (verification): no unauthorized frozen-contract drift** — every
frozen-path change since `w2-complete` is additive-only (0 deletions) and
ADR-covered; independently confirms the contract-guardian pass above.

**D2 (LOW): ADRs 0009/0011 still read "Status: Proposed"** though merged and
live. **Fixed this session** (both flipped to Accepted).

### Surfaces that refuted cleanly (gate evidence)

The refute-mode pass could not break: webhook signature verification
(HMAC-SHA256 over raw body, constant-time compare, fail-closed on missing
secret), webhook org attribution (server-bound `custom_data.org_id` from the
checkout session; UUID-validated; upsert `setWhere` pins org), replay /
out-of-order events (occurred-at monotonic guard ⇒ idempotent no-op),
entitlement bypass (free band enforced in `handleApi` *and* page layout from
the same `computeAccess`; missing subscription fails closed to free-and-counted),
metering double-billing (row-locked compare-and-set, under-bill-and-self-heal
failure mode), share-link LIST/DELETE ownership (W2 Finding-2's
`person.authUserId === ctx.user.id` guard present on **all three** verbs;
revoked tokens stop resolving immediately), checkout/portal (admin-only,
org-scoped Paddle-id lookup, `no-store`), audit/health (org-pinned list,
admin-only route; health is unauthenticated but returns only liveness),
tripwires (none introduced).

## Sign-off

- [x] Deploy plumbing + all 7 `PADDLE_*` secrets live in production
      (founder added the final two, 2026-07-08; re-deploy confirmed green,
      checkout/webhook probes fail-closed as expected).
- [ ] Do these numbers match reality? — founder
- [ ] Would I trust this score? — founder

**GATE STATUS: PASS.** All fix-now findings applied and re-verified; the two
production-deploy blockers this pack originally flagged are both closed.

Outstanding, explicitly founder-only and **not** blocking the pass:
1. End-to-end Team purchase walkthrough on prod (checkout overlay → webhook →
   entitlement flips → portal), incl. confirming the Paddle dashboard
   "Default Payment Link" setting.
2. Measured TTFI run (signup → first score with a real key) to back the
   "minutes" landing claim.
3. Run `/code-review ultra` for the independent cloud pass.
4. (Launch-content, non-blocking for the gate) return
   `docs/launch/benchmark-post-data-needs.md` answers to unblock the W3-P
   benchmark post; record vendor payloads for the golden dogfooding test.
5. Fast-follow: thread honesty gaps into the team dashboard (A5 above;
   tracked as a spawned task).
