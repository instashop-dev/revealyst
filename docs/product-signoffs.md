# Product sign-off ledger

A durable home for founder product decisions. Nothing here existed before W9 closure
T0.5 — `docs/approvals.md` covers external filings (GitHub App, Paddle, legal) only, not
product-call outcomes. Entries are added as decisions are made or defaults are applied;
"default applied" rows stand until the founder ratifies or overrides them (CLAUDE.md
rule 4 — founder judges evidence, human gates are never self-certified).

| Date | Item | Decision | Evidence link | Status |
|---|---|---|---|---|
| 2026-07-17 | Remove the polled admin-API connectors; go agent-first (usage-source model) | **Founder: remove them now.** The product measures AI capability/adoption/workflows from behavioral signal *inside* the tools (captured by the desktop agent), not from LLM APIs — so the polled connectors (Anthropic Console, OpenAI, Cursor, GitHub Copilot) measure the wrong surface. Two founder-chosen constraints: **existing connector data frozen in place** (no destructive migration, no retroactive billing/history change; `renewalDate`/`renewal_reminder_state`/`VENDOR_IDS` left inert) and the device surface **relocated to Settings → Devices** (no new nav route). **Accepted tradeoff:** coverage narrows to Claude Code until the agent gains Cursor/Codex/ChatGPT/Claude-Desktop connectors (separate gated work). | `docs/decisions/0056-remove-polled-connectors-agent-first.md`; branch `remove-polled-connectors`; empty `src/connectors/index.ts` + worker dispatch removed; `/connections` route + connect UI deleted; onboarding/landing rewritten agent-first | **Ratified 2026-07-17** — reverses the connector halves of ADR 0013/0022/0032; no migration, no frozen path touched; connector modules kept as inert test fixtures (dead-code deletion is an optional follow-up) |
| 2026-07-16 | OQ-001 — exit-gate N / threshold for the §14 digest-return metric | **Founder: exit-gate window N = 1 week** (override of the 6-week default). **Ratio bar (signed later the same day, delegated to the TCI-analysis recommendation): trailing-1-week `companion_revisit / digest_return` ratio ≥ 2.0, denominator non-empty.** Rationale: the digest CTA click fires BOTH events, so ratio 1.0 = all companion traffic is email-driven; ≥ 2.0 = at least half of companion engagement is voluntary (no email prompt) — which is the §14 voluntary-return bet stated as a number. Coarse aggregate index (streams carry no identity), not a per-user funnel. | `docs/Revealyst_Closure_Execution_Plan.md` §6 D1; `scripts/digest-return-rate.ts` (`DEFAULT_WEEKS = 1`, signed bar noted in output); `src/lib/digest-return-rate.ts` ratio semantics | **Ratified in full 2026-07-16** — window = 1 week; bar = ratio ≥ 2.0 (founder adopted the recommended bar) |
| 2026-07-16 | OQ-002 — Custom Index Builder demotion ("build your own custom scorecard") | **Founder: keep it; stays demoted, do not promote for now** (route intact) — matches default | `docs/Revealyst_Closure_Execution_Plan.md` §6 D2; code already matches (route live, not promoted) | **Ratified 2026-07-16** — matches default; no code change |
| 2026-07-16 | OQ-008 — third-ladder confirmation (capability profile card) | **Founder-confirmed: capability profile = breakdown of the existing band, NOT a new/third scoring system** (confirms ADR 0036) | ADR `docs/decisions/0036-user-capability-state.md`; `docs/product/requirements.csv` OQ-008 row | **Ratified (confirmed) 2026-07-16** — no code change; matches ADR 0036 |
| 2026-07-16 | D11 — TEL-012 (context-usage / context-window signal): build now vs. formally Future | **Founder: build it now** (override of "move to Future") | `docs/Revealyst_Closure_Execution_Plan.md` §6 D11, T4.2; TEL-012 build PR (ADR + migration) | **Ratified (override) 2026-07-16** — scheduled and built; see the TEL-012 follow-up PR |
| 2026-07-16 | D4 — Companion / dashboard card consolidation (fewer, bigger cards) | **Founder: decide per the deep-research report** → adopt its direction (fewer/larger cards, progressive disclosure, one dominant action per screen) | `docs/Revealyst_UIUX_Execution_Plan.md` U1 (Today/Growth split) carries it; supersedes the narrow T3.3 option | **Ratified 2026-07-16** — direction adopted; execution lands via UI/UX plan U1 (not built in this PR) |
| 2026-07-16 | D5 — Audit-log scope widening: log routine business/system events (e.g. subscription changes) | **Founder: yes in principle, but do not build it for now** | `docs/Revealyst_Closure_Execution_Plan.md` §6 D5, T2.4 | **Ratified 2026-07-16** — affirmed in principle, deferred; no machine-event audit rows written this wave |
| 2026-07-16 | D7 — Budget/renewal email opt-out policy | **Founder: keep as is** — transactional, no opt-out (matches default) | `docs/Revealyst_Closure_Execution_Plan.md` §6 D7, T3.5b | **Ratified 2026-07-16** — matches default; no change |
| 2026-07-16 | D8 — "Invite N more" cold-start copy | **Founder: keep the current honest-but-generic message** (no new nudge copy) | `docs/Revealyst_Closure_Execution_Plan.md` §6 D8, T3.5a | **Ratified 2026-07-16** — matches default; no change |
| 2026-07-16 | D9 — Formally promise mobile support + a WCAG 2.1 AA target in the spec | **Founder: don't make the promise** — spec stays silent (matches default). a11y work continues as engineering practice, just not a contractual claim. | `docs/Revealyst_Closure_Execution_Plan.md` §6 D9, T0.4 | **Ratified 2026-07-16** — matches default; no spec claim added |
| 2026-07-16 | D10 — `--muted-foreground` contrast-token darken (visual pass) | **Founder: darken it** (override of "token unchanged") | `src/app/globals.css` `:root --muted-foreground` oklch(0.556 → 0.44); `docs/Revealyst_Closure_Execution_Plan.md` §6 D10, T2.6 item 6 | **Ratified (override) 2026-07-16** — light-mode token darkened to ~6.6:1 on white; dark mode unchanged |
| 2026-07-16 | T2.5 — Frozen-contracts guard semantics (option A vs B) | **Founder: keep it as it is** — option A (current guard + human-review backstop), do not make it stricter | `docs/Revealyst_Closure_Execution_Plan.md` §6 D6, T2.5; guard comment in `ci.yml` | **Ratified 2026-07-16** — matches default; option B (registration checker) stays an optional, un-adopted follow-up |
| 2026-07-16 | D12 — Exec memo: in-app page vs email + export only | Default: email + export only (status quo) | `docs/Revealyst_Closure_Execution_Plan.md` §6 D12; code already matches (`/api/exec-report` export + monthly email, no in-app page) | **Ratified 2026-07-16** — founder confirmed: email + export only, no in-app page |
| 2026-07-16 | D-TCI-1 — May a manager see named per-person capability data (matrix, member profile, expert directory, mentor matches)? | Default: **no** — self-view-only mastery stands (ADR 0036/0038; V4 NOT-list). If ever reversed, follow the ADR-0038 mold (founder-signed ADR + consent machinery + authz test matrix). | `docs/product/tci-gap-analysis.md` §4, conflict C1; governing ADR (written 2026-07-16): `docs/decisions/0045-manager-per-person-capability-and-spend-visibility.md` | **Ratified (override) 2026-07-16 — Founder: YES**, managers may see named per-person capability data. Build still requires the ADR-0038-mold ADR first (consent machinery in the visibility framework, per-surface identity registration, manager-vs-member authz test matrix, purge/tenant registrations) — the sign-off unblocks scheduling, the ADR governs the how. Unlocks conflict C1 (~20 requirements). |
| 2026-07-16 | D-TCI-2 — May a manager see named per-person spend? | Default: **no** — spend stays aggregate; TCI §10 itself requires an admin toggle, so the default must be off | `docs/product/tci-gap-analysis.md` conflict C2; governing ADR (written 2026-07-16): `docs/decisions/0045-manager-per-person-capability-and-spend-visibility.md` | **Ratified (override) 2026-07-16 — Founder: YES**, managers may see named per-person spend — behind the TCI §10 admin visibility toggle, default OFF, honestly labeled with allocation confidence (per-model dollar splits stay excluded: no vendor reports them, invariant b). Unlocks conflict C2. |
| 2026-07-16 | D-TCI-3 — Add a manager role tier (and optionally executive/observer) to org membership? | Default: **not yet built** — org role is admin/member only; this is a structural gap, not a privacy reversal | `docs/product/tci-gap-analysis.md` conflict C8; `src/lib/api-context.ts` (role enum) | **Ratified 2026-07-16 — Founder: YES**, add a manager role tier. Phase 2 of the TCI roadmap is unblocked (manager role → team settings → capability history → aggregate surfaces). |
| 2026-07-16 | D-TCI-4 — Adopt TCI's 0–100 score + 5-level "inactive→expert" ladder, or keep the shipped positive-first 4-band + demoted diagnostics? | Default: **keep the 4-band** (OQ-008 "breakdown of the existing band, never a competing ladder"; raw score stays demoted per U1/D4) | `docs/product/tci-gap-analysis.md` conflict C7; `src/lib/capability-glossary.ts` | **Ratified 2026-07-16 — Founder: keep the 4-band** (matches default/recommendation). TCI's 5 levels map onto the shipped positive-first bands; no 0–100 hero, no "inactive/beginner" vocabulary. |
| 2026-07-16 | D-TCI-5 — Expand nav into a "Team Intelligence" area (11 sub-items) or keep the 5-card single-dashboard fold? | Default: **keep the fold** — extend the 5 cards with cards/drawers (minimal-by-default; W5 ~18-panels→5-cards; D4) | `docs/product/tci-gap-analysis.md` conflict C6; `src/lib/nav-items.ts` | **Ratified 2026-07-16 — Founder: keep growing the 5-card page** (matches default/recommendation). TCI screens land as cards/drawers on the existing team dashboard, not new nav items. |
| 2026-07-16 | D-TCI-6 — Persist per-capability team history (append-only periodic rollup) to power trends and coaching baselines? | Default: **not built** — a deliberate exception to compute-on-read; needs an ADR; without it §6.5 Growth trends stay unbuildable | `docs/product/tci-gap-analysis.md` conflict C9; governing ADR (written 2026-07-16): `docs/decisions/0046-per-capability-team-history-rollup.md` | **Ratified (override) 2026-07-16 — Founder: YES**, persist per-capability team history. Needs its own ADR (deliberate compute-on-read exception; derive snapshots from the same pure functions the dashboard uses + shared-source parity test). Unblocks §6.5 Growth trends, movement counts, coaching baselines. |
| 2026-07-16 | W6-A — companion-in-team-orgs dogfood gate: hold or accelerate given the TCI decisions? | **Founder: take the recommended approach** — the gate STANDS. The ~6-week dogfood clock (running since 2026-07-14, matures ~2026-08-25) is judged on the now-fully-signed OQ-001 metric (1-week window, ratio ≥ 2.0). The aggregate team layer and the D-TCI-3/6 foundations proceed in parallel; per-person-in-team-org surfaces wait for the clock + the §9.4 sub-case-C ADR. | `docs/product/tci-gap-analysis.md` §2/§9 (Phase 3); CLAUDE.md W6-A gate | **Ratified 2026-07-16** — gate unchanged; parallel aggregate work approved |
| 2026-07-16 | D-U1 — Approve the Today/Growth split of the personal companion (UI/UX plan R6) | Default: proceed (plan's central bet) | `docs/Revealyst_UIUX_Execution_Plan.md` §6 D-U1; built in U1 — `src/lib/nav-items.ts` PERSONAL_NAV_ITEMS, `/growth` route | **Ratified 2026-07-17** — founder confirmed; U1 shipped per default |
| 2026-07-16 | D-U2 — Nav label "Today" for `/dashboard` personal view (route unchanged, R10) | Default: proceed | `docs/Revealyst_UIUX_Execution_Plan.md` §6 D-U2; `src/lib/nav-items.ts` PERSONAL_NAV_ITEMS | **Ratified 2026-07-17** — founder confirmed |
| 2026-07-16 | D-U3 — Settings consolidation incl. folding `/members`, `/account`, `/billing` (U3) | Default: proceed; old routes redirect | `docs/Revealyst_UIUX_Execution_Plan.md` §6 D-U3; `src/app/(app)/account,billing,members,teams,people/page.tsx` (308s), `src/app/(app)/settings/*` | **Ratified 2026-07-17** — founder confirmed **including the admin-only rider** on `/settings/people` (the access-narrowing is now founder-approved, not just flagged) |
| 2026-07-16 | D-U4 — Bottom navigation on mobile (report mandates; current sheet-sidebar is compliant) | Default: **not adopted** — sheet sidebar + `SidebarTrigger` stays | `docs/Revealyst_UIUX_Execution_Plan.md` §6 D-U4 | **Ratified 2026-07-17** — founder confirmed — not adopted; sheet sidebar stays; revisit with real mobile-usage data |
| 2026-07-16 | D-U5 — Benchmark consent toggle moves to Settings → Privacy | Default: proceed | `docs/Revealyst_UIUX_Execution_Plan.md` §6 D-U5; `src/app/(app)/settings/privacy` hosts `BenchmarkConsentToggle` | **Ratified 2026-07-17** — founder confirmed |
| 2026-07-16 | D-U6 — Persistent Help entry in the shell (report wants same-place help everywhere) | Default: not adopted this wave; InfoTips + contextual pages remain | `docs/Revealyst_UIUX_Execution_Plan.md` §6 D-U6 | **Ratified 2026-07-17** — founder confirmed — not adopted this wave |
| 2026-07-16 | D-U7 — Playbooks future direction (R2) | Default: Future ledger only | `docs/Revealyst_UIUX_Execution_Plan.md` §6 D-U7, §8 | **Ratified 2026-07-17** — founder confirmed — Future ledger only |
| 2026-07-16 | D-DA-1 — Desktop Agent: resident-collector go/no-go (SYNC-007 / TEL-017 / OQ-6) | Default: **blocked** — the resident agent was demoted pending `last_success_at` cadence telemetry; building it needs an explicit founder green-light. Until signed, only the M0–M1 non-collecting foundation proceeds (repo enablement + app shell; no data collection, no pairing); no product-behavior PR merges. | `docs/Revealyst_Desktop_Agent_Execution_Plan.md` §6; `docs/product/desktop-agent-gap-analysis.md` C1; `docs/research/2026-07-09-desktop-collector.md` | **Ratified 2026-07-17** — founder re-confirmed the in-session sign-off on review |
| 2026-07-16 | D-DA-2 — Desktop Agent: sub-case-C ADR (Spec V4 §9.4 a/b/c) authorizing Team-org desktop enrollment | Default: **Personal orgs only** — desktop data self-view-only; Team-org enrollment UI never rendered until the ADR (exclusion audit predicate + dual-source dedup + surfaced-not-billed) is founder-signed on the ADR-0038 mold. | `docs/product/desktop-agent-gap-analysis.md` C2; Spec V4 §9.4; POP-007 | **Ratified (override) 2026-07-17 — Founder: the desktop agent lives in ONE enrollment context only, not two.** Choice of which delegated to the orchestrator; decided: **personal workspace only** (matches shipped behavior per ADR 0047, the individual-first V4 thesis, and the self-view-only privacy posture — device telemetry lands in the user’s own workspace; team surfaces get value via the established aggregation paths). Team-org desktop enrollment is CLOSED, not deferred — the sub-case-C device-enrollment ADR is no longer needed |
| 2026-07-16 | D-DA-3 — Desktop Agent: day-aggregate ingestion (adaptation of spec §12 per-event server schema) | Default: **day-aggregate stands** — desktop batches ride the existing `AgentIngestRequest` + content-addressed idempotency (founder-directed thin-collector architecture, research §5.6); per-event server storage goes to the Future ledger. | `docs/product/desktop-agent-gap-analysis.md` C3; ADR 0002 | **Ratified 2026-07-17** — daily-summary uploads confirmed, **plus a founder scope addition: support on-demand manual upload** (user-triggered sync in addition to the daily cadence). Desktop-agent workstream to pick up the manual-upload addition |
| 2026-07-16 | D-DA-4 — Desktop Agent: static `rva1.` device token in Phase 1 (deviation from spec §22.1 short-lived + rotating tokens) | Default: **deviation stands until M7** — prod-proven scheme (rotate on re-mint, pause = revoke, OS-keychain storage); token rotation lands as hardening task T7.2, not an MVP blocker. | `docs/product/desktop-agent-gap-analysis.md` C4; `src/lib/agent-token.ts` | **Ratified 2026-07-17** — static `rva1.` device token accepted for Phase 1; rotation stays the recorded follow-up |
| 2026-07-16 | D-DA-5 — Desktop Agent: prompt-feature extraction boundary — may the extractor read prompt TEXT on-device (emitting only bounded enums/counts), beyond today's shape-only reads? | Default: **shape + counts only** — classifier fields stay `unknown`; the feature-signal contract ADR (plan T5.2) is blocked. Research §5.6: richer payloads are a new founder privacy analysis, not an incremental widening. | `docs/product/desktop-agent-gap-analysis.md` C8; `src/lib/agent-collection-schema.ts` (`content_block_type` precedent) | **Ratified (override) 2026-07-17 — Founder: YES**, the extractor may read prompt TEXT on-device, emitting ONLY metadata (bounded enums/counts — never the text, never excerpts). The no-prompt-content-ingestion tripwire still binds the SERVER: raw text must never leave the device; the on-device boundary + emitted-shape allowlist need their own ADR in the desktop workstream before anything text-reading ships. **That ADR is now written: `docs/decisions/0055-desktop-feature-signals.md` (D-DA-9) — the on-device classification boundary + emitted-shape allowlist are specified; classifier build gated on ratifying its key set.** |
| 2026-07-16 | D-DA-6 — Desktop Agent: auto-update acceptability for individually-installed agents (BetrVG change-disclosure note, research R2) | Default: **proceed for Personal orgs** (user-controlled install, signed staged updates); Team-org devices inherit D-DA-2's gate. | `docs/Revealyst_Desktop_Agent_Execution_Plan.md` §6; `docs/research/2026-07-09-desktop-collector.md` R2 | **Ratified 2026-07-17** — auto-update confirmed (works-council change-disclosure note to ride along per research R2) |
| 2026-07-16 | D-DA-7 — Desktop Agent: Windows code-signing cert type (EV vs OV) + Apple Developer account owner | Default: **EV recommended** (avoids SmartScreen reputation lag); blocked on founder purchase either way — file both requests now (rule 5). | `docs/Revealyst_Desktop_Agent_Execution_Plan.md` T0.4; `docs/approvals.md` | **Ratified (deferral) 2026-07-17** — founder will buy certificates later; for now testing proceeds on UNSIGNED installs (dev machines only — unsigned builds are never distributed to end users) |
| 2026-07-17 | D-DA-8 — Desktop Agent: connector-scoped ingest window-delete (safe multi-connector-per-device sync) | Default: **live export-import→sync GATED** — the hardened Claude-export importer ships in a projection-only (no-enqueue) state (T5.1's live Claude Code connector is unaffected). Enabling live import→sync safely requires **connector-scoped** `deleteWindowForConnection` + `sourceConnector` in the `metric_records` natural key + a scoring cross-connector merge decision — a frozen-contract change rippling across ALL connectors + the scoring engine (rules 1/4), not a desktop-only change. Discovered in the T5.3 adversarial review: the server window-delete is connection-scoped, so a second connector (`claude_export`) syncing through the shared device connection would clobber the live connector's overlapping-day metrics. | `docs/product/desktop-agent-spec.md` §11.3.2; T5.3 PR #283; `src/db/org-scope/metrics.ts` `deleteWindowForConnection`; `src/db/schema/tracking.ts` `metric_records` natural key | **Pending** — importer built + hardened + adversarially reviewed + merged (projection-only); live import→sync unblocks after the connector-scoped-ingest ADR |
| 2026-07-17 | D-DA-9 — Desktop Agent: feature-signal contract (the D-DA-5 "own ADR" prerequisite) — define the content-free non-eng signal keys + the on-device classification boundary before any prompt-text-reading classifier ships | **Specified (ADR-only, no code/migration yet).** ADR 0055 records an honest viability assessment (Part 1: the `no browser extension/proxy` tripwire makes the majority of non-dev AI use — browser-based ChatGPT/Claude/Gemini/Copilot — permanently unobservable; near-term collectable = coarse native-AI-app adoption only; task-category classification is buildable under D-DA-5 but data-starved until native apps expose readable local prompt logs) and defines the contract (Part 2: four content-free keys `ai_tool_used`/`task_category`/`iteration_depth`/`verification_behavior`, a closed task-category enum, borrow-and-drop on-device classification, fail-closed enforcement on device+server, `directional` ceiling). The feature-signal contract is now specified; the classifier build, the metric-key seed migration, and the per-pack activation are the authorized downstream steps. | `docs/decisions/0055-desktop-feature-signals.md`; gate row D-DA-5 above; `docs/product/noneng-role-packs-spec.md` §3b/§7; ADR 0056 §Deferred item 2; plan T5.2 | **PAUSED by founder 2026-07-17** — after reviewing the honest viability picture (thin; the majority of non-dev AI use is browser-based and permanently unobservable under the `no browser extension/proxy` tripwire), the founder chose to **pause the downstream build (P10 metric-key seed, P11 Rust classifier, P12 pack activation) and revisit if/when the browser-collection rule changes.** The contract (ADR 0055) stands as the durable record so a future resume is cheap — no key set is ratified, no code ships, no classifier reads prompt text. The non-eng packs (ADR 0056) remain dormant / `not_measured` by design. **RESUMED — gate cleared by founder 2026-07-18:** the founder un-paused the D-DA-5/D-DA-9 downstream build (Recommendation #9). The classifier build now ships — P10 metric-key seed (`drizzle/0046_seed-worktype-metrics.sql`) + P11 Rust classifier (`desktop-agent/src-tauri/src/extract/classify.rs`), governed by `docs/decisions/0059-worktype-classifier.md`. It stays fully content-free (reads prompt text ON-DEVICE only, emits closed-enum labels + counts, the words never leave the machine), keeps the `directional` ceiling, and is **wired-not-live**: LIVE emission is still gated on capturing a real fixture (rule 2), and P12 per-pack activation is a separate gated PR. **This resume was relayed via the workstream directive and is recorded here so the durable ledger matches the build — founder to ratify this row.** |
| 2026-07-17 | D-ONB-1 — User-facing team-workspace creation: who can create team workspaces and how do people join? | **Founder: workspace ADMINS (not platform admins) can create team workspaces and invite people; follow best practices.** Every user is admin of their own personal workspace, so in practice any user can start a team workspace and become its admin; members join via the existing invite flow. Supersedes the platform-admin-only posture of the P4 unblock (PR #285), which remains as internal tooling. | `docs/product/tci-gap-analysis.md` §9 (team onboarding left open); P4 PR #285 body | **Ratified 2026-07-17** — build started same day |
| 2026-07-17 | D-TCI-7 — Manager notes (private notes a manager writes about a team member) | **Founder: build it.** Constraints carried from the TCI analysis + ADR 0045 posture: org-scoped new table (three registrations + its own ADR), manager-of-that-team authors/reads (same access derivation as the drill-in), notes NEVER feed scoring (pin with a test), purge-registered, visible in the drill-in only, plain-English copy. | `docs/product/tci-gap-details.md` TCI-PRIV-004; `docs/decisions/0045-...md`; built: `docs/decisions/0053-manager-notes.md` + mig `0043_manager-notes` | **Ratified 2026-07-17** — queued behind the team-onboarding build |
| 2026-07-17 | D-TCI-8 — Non-engineering role packs (Product/Marketing/Sales/CS/HR/Finance/Operations): override the V4 NOT-list ("no non-eng role libraries in MVP/V1") to bring the packs *into scope* as honest scaffolding, given the desktop agent (D-DA-5) is now a plausible telemetry source? | **Default (NOT-list): blocked** on OQ-003/OQ-004 (no honest non-eng telemetry source exists). **Proposed override:** bring the packs in as DEFINITIONS + architecture-readiness only — role→domain link, seven pending (`is_active=false`) domains/roles, and a Marketing proof pack bound to ZERO signals. **Honest-empty-until-desktop-source posture:** nothing renders on a live surface and no non-eng capability can produce a computed score (engine "no evidence → no row" + the `is_active` gate); this scaffolding is safe under the current default. **Hard dependency:** ACTIVATION (flip `is_active`, bind signals, domain-scope the graph read) requires (1) founder ratification of this override AND (2) the desktop-agent feature-signal-contract ADR that governs the D-DA-5 metadata emission — raw prompt text must never leave the device. | Part A spec `docs/product/noneng-role-packs-spec.md`; `docs/decisions/0054-noneng-role-packs-groundwork.md` + mig `0044_noneng-role-packs`; honesty guard `tests/capability-noneng-honesty.test.ts`; gate row D-DA-5 above | **Founder-directed 2026-07-17** — the founder instructed "add non-engineering role packs," which IS the NOT-list override (scope now includes non-eng packs). The honest-gated scaffolding shipped per that directive (safe-by-default: no live surface, no fabricated score — the engine + `is_active` gate make that structural). Still separately gated: **ACTIVATION** (flipping packs live) needs the desktop-agent feature-signal-contract ADR (D-DA-5 metadata emission) before any non-eng capability can honestly score — that's a data-source prerequisite, not a further permission. |
| 2026-07-19 | OQ-TMD-1 — Team Manager Dashboard: does the "Manager Command Center" homepage restructure stay within D-TCI-5 (grow the 5-card page, no new nav), or reopen D-TCI-5 to add nav areas (analysis §4 Overview/Capabilities/Initiatives/Team/Settings)? | Default (plan): **stay within D-TCI-5** — restructure `/dashboard` into conclusion-first sections + drawers; at most ONE new `MANAGER_NAV_ITEMS` entry for Initiatives if P2 proves it needs a route. `/maturity` folds in as a lens (route stays alive). | `docs/Revealyst_Team_Manager_Dashboard_Execution_Plan.md` §1 R1, §3 P0; D-TCI-5 (row above) | **Pending** — no build proceeds past P0 IA if it would add nav areas without reopening D-TCI-5 |
| 2026-07-19 | OQ-TMD-2 — Team goal: may a manager-set team goal **bias** the recommendation ranking, or only display alongside it? | Default (plan): **display + tie-break only** — the goal metric tie-breaks the existing impact sort; it never changes which recs are eligible, and with no goal set the priority order is byte-identical to today (migration-equivalence guard). | plan §3 P1 (T1.3, OQ-TMD-2); `src/lib/score-insights.ts` `deriveAttention` | **Pending** — P1 stops at the guard until ratified |
| 2026-07-19 | OQ-TMD-3 — Copy relabels (analysis §12/§13), especially **Efficiency → "License utilisation"** (honesty-motivated: "efficiency" implies productivity, which the product refuses to claim — invariant b) | Default (plan): **proceed — labels only, score slugs frozen** (`adoption`/`fluency`/`efficiency` slugs untouched; only rendered copy changes in `src/lib/team-overview-copy.ts` + score-card copy) | plan §1 R6, §3 P0 (T0.5) | **Pending** |
| 2026-07-19 | OQ-TMD-4 — "Ask Revealyst" (analysis §14): approve a fixed canned-question set for V1, each answered by an existing pure read; free-text natural-language Q&A explicitly Future | Default (plan): **canned questions only, no LLM, no DSL** (tripwire G7). Free-text Q&A deferred to the Future ledger. | plan §1 R5, §3 P5 (T5.4) | **Pending** |
| 2026-07-19 | OQ-TMD-5 — Intervention effectiveness (analysis §14): descriptive before/during/after only, or a real holdout experiment registered in the (empty) `EXPERIMENTS` registry? | Default (plan): **descriptive before/after only, no causal claim** (banned-phrasing test enforces "do not claim causality"); holdout only if the founder wants it. | plan §3 P3 (T3.4); `src/lib/experiments.ts`, `recommendation_exposure` | **Pending** |
| 2026-07-19 | OQ-TMD-6 — "By workflow" breakdown (analysis §9): confirmed **Future** until a workflow telemetry source exists? | Default (plan): **Future** — there is NO workflow data model today (`workflow-diversity.ts` is a feature-diversity count, not named workflows); "by workflow" is tied to the paused D-DA-5/D-DA-9 non-eng feature-signal contract. No workflow entity built under this plan. | plan §3 P4 (T4.3), §7; gate rows D-DA-5/D-DA-9 (above) | **Pending** |
| 2026-07-19 | OQ-TMD-7 — Monthly manager review (analysis §14): reuse `composeExecReport` output in an in-app manager card, or build a manager-specific composer? | Default (plan): **reuse `composeExecReport`** in a manager review card (D12 keeps the exec *memo* email+export; this is a separate manager surface). | plan §3 P5 (T5.2); `src/lib/exec-report.ts`; D12 (row above) | **Pending** |

## Notes

- "Default applied" means the plan's documented fallback is what the code/docs currently
  reflect — no new work was withheld waiting on the founder, but the decision is not yet
  formally ratified.
- "Pending" (OQ-001/002/008) means no default behavior change has shipped beyond what the
  code already does; the row exists so the open question has one citable home instead of
  being re-derived every wave.
- When the founder ratifies or overrides a row, update its Status column in place (do not
  delete the row — it is the record of what was decided and when) and cross-link the
  ratifying conversation/PR if one exists.
